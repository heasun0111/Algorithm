#원소 삭제 후, U/D생각
def solution(n, k, cmd):
    N=range(n)
    trash=[]
    idx=k
    for i in range(len(cmd)):
        tmp=cmd[i]
        if len(tmp)==1:
            if tmp=="C":


            elif tmp=="Z":

        else:
            tmp=tmp.split()


    answer = ''
    return answer